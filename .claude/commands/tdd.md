Activate the `tdd-guide` agent for the following feature or bug:

$ARGUMENTS

Follow the strict TDD sequence:
1. Read the interface/types (NOT the implementation)
2. Write failing tests — happy path + error cases
3. Run tests — confirm they fail
4. Implement minimum code to pass
5. Run tests — confirm all pass
6. Refactor only if needed (tests must stay green)

Do NOT skip any phase. Report the result of each test run.
