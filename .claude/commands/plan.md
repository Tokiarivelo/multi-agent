Activate the `planner` agent for the following task:

$ARGUMENTS

Follow the planning protocol:
1. Grep for relevant symbols/files (do NOT read everything)
2. Read only the directly relevant files
3. Produce a sequenced plan: Goal / Sequence / Risks / Out of Scope
4. Max 7 steps — split into phases if larger
5. Do NOT write any implementation code
