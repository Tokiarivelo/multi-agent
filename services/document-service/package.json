{
  "name": "@multi-agent/document-service",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "uvicorn main:app --host 0.0.0.0 --port 3009 --reload",
    "start": "uvicorn main:app --host 0.0.0.0 --port 3009",
    "build": "echo 'Python service — no build step'",
    "lint": "echo 'Python service — run: flake8 .' || true",
    "test": "echo 'Python service — run: pytest' || true",
    "install:py": "pip install -r requirements.txt"
  }
}
